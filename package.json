{
  "name": "holded-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip3 install pymupdf --break-system-packages --quiet || pip install pymupdf --break-system-packages --quiet"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.0",
    "fontkit": "^2.0.2",
    "node-qpdf2": "^6.0.0",
    "pdf-lib": "^1.17.1"
  }
}